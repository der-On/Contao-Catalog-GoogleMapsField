This module adds a google maps field type to the catalog-module. With it you can use a google map with a draggable marker in the backend to set latitude, longitude and zoom parameters for a catalog item.

The files need to be placed within the folder system/modules/cataloggooglemapsfield/